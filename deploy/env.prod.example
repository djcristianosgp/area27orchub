# Banco de dados
POSTGRES_USER=orchub_user
POSTGRES_PASSWORD=orchub_password
POSTGRES_DB=orchub_db

# Imagens no registro
IMAGE_BACKEND=docker.io/<usuario>/are27orchub-backend:latest
IMAGE_FRONTEND=docker.io/<usuario>/are27orchub-frontend:latest

# Backend
JWT_SECRET=defina_um_segredo_unico
JWT_EXPIRATION=24h

# Frontend -> URL da API (ajuste conforme ambiente)
# Local VPS (porta 3000):
# VITE_API_URL=http://seu-vps:3000
# Domínio:
# VITE_API_URL=https://api.seu-dominio.com
VITE_API_URL=http://localhost:3000
